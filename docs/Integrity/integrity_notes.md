1. Use Git Bash
2. Navigate to /data/raw
3. Use the following command:
    sha256sum.exe --check ../../docs/Integrity/checksums.sha256
4. If the integrity of the file hasn't changed the command will report "OK" for each file where that applies.